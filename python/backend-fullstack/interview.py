# Structure JSON blocks
    # lines
        words
# Block Two would have the red writing
# Given JSON data return first and last name
# Stored top down
# import json
# open json file
    # Check if LN and FN is an appearing work
    # Otherwise check 
        # Walk json to line with word Apply
        # store last name which should the the word after apply
        # store first name which should the the 2 word after apply
   
